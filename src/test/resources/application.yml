spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:ImageManager;MODE=MYSQL;DB_CLOSE_DELAY=-1"
    username: sa
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create